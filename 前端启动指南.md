# JeecgBoot Vue3 前端项目启动指南

## 项目信息
- **前端框架**: Vue 3 + Vite + Ant Design Vue
- **开发端口**: 3100
- **后端API**: http://localhost:8080/jeecg-boot

## 环境要求
✅ **Node.js**: v20.18.0 (已安装，符合要求 ^18 || >=20)  
✅ **npm**: 10.8.2 (已安装)  
✅ **项目依赖**: 已安装  

## 启动方法

### 方法1：使用启动脚本（推荐）
```bash
./start-frontend.sh
```

### 方法2：手动启动
```bash
# 进入前端项目目录
cd jeecgboot-vue3

# 启动开发服务器
npm run dev
```

### 方法3：使用pnpm（如果安装了pnpm）
```bash
cd jeecgboot-vue3
pnpm dev
```

## 环境配置

### 开发环境配置 (.env.development)
- **后端API地址**: http://localhost:8080/jeecg-boot
- **前端代理配置**: 已配置跨域代理
- **Mock数据**: 已启用

### 端口配置 (.env)
- **前端端口**: 3100
- **应用标题**: JeecgBoot 企业级低代码平台

## 访问地址

启动成功后，可以通过以下地址访问：
- **前端应用**: http://localhost:3100
- **默认账号**: admin / 123456

## 启动成功标志

当看到类似以下输出时，表示启动成功：
```
  VITE v6.x.x  ready in xxx ms

  ➜  Local:   http://localhost:3100/
  ➜  Network: http://xxx.xxx.xxx.xxx:3100/
  ➜  press h to show help
```

## 功能特性

- ✅ **热重载**: 代码修改后自动刷新
- ✅ **代理配置**: 自动代理后端API请求
- ✅ **Mock数据**: 支持模拟数据开发
- ✅ **TypeScript**: 完整的类型支持
- ✅ **组件库**: Ant Design Vue 4.x

## 开发建议

1. **确保后端服务运行**: 前端需要后端API支持
2. **使用现代浏览器**: 推荐Chrome、Firefox、Safari最新版本
3. **开发工具**: 推荐安装Vue DevTools浏览器扩展

## 常见问题

### 1. 端口被占用
如果3100端口被占用，可以：
```bash
# 查看端口占用
lsof -i :3100

# 或者修改 .env 文件中的端口
VITE_PORT = 3101
```

### 2. 依赖安装失败
```bash
# 清理缓存重新安装
cd jeecgboot-vue3
rm -rf node_modules package-lock.json
npm install
```

### 3. 跨域问题
项目已配置代理，如果仍有跨域问题，检查：
- 后端服务是否在8080端口运行
- .env.development中的API地址配置

### 4. 构建问题
```bash
# 清理构建缓存
npm run clean:cache
npm run dev
```

## 项目结构

```
jeecgboot-vue3/
├── src/
│   ├── api/          # API接口
│   ├── components/   # 公共组件
│   ├── views/        # 页面组件
│   ├── router/       # 路由配置
│   ├── store/        # 状态管理
│   └── utils/        # 工具函数
├── public/           # 静态资源
├── .env              # 基础环境变量
├── .env.development  # 开发环境配置
└── vite.config.ts    # Vite配置
```

## 下一步

1. 启动前端项目：`./start-frontend.sh`
2. 确保后端服务运行在8080端口
3. 访问 http://localhost:3100
4. 使用 admin/123456 登录

现在可以开始开发了！🚀
